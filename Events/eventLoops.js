// The event loops are the only way that asynchronous javascript is made possible
// The event loops is the process that monitors both the callback or the event queue and the callstack

// The event loop flowchart is in 3parts: the callback queue and the js runtime and the web Api
// The js runtime has the call stack and the Heap
// The web api has the Api and the ajax
